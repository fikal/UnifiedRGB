<UserControl x:Class="UnifiedRgb.App.Controls.PaletteStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:UnifiedRgb.App.Controls">
    <!-- The current target's palette as removable swatches. One control for the
         three byte-identical strips the lighting pane used to carry (palette
         effects, custom pattern, key ripple). Click a swatch = remove it. -->
    <ItemsControl ItemsSource="{Binding PatternPalette}">
        <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Width="{Binding SwatchSize, RelativeSource={RelativeSource AncestorType={x:Type controls:PaletteStrip}}}"
                        Height="{Binding SwatchSize, RelativeSource={RelativeSource AncestorType={x:Type controls:PaletteStrip}}}"
                        Margin="{Binding SwatchMargin, RelativeSource={RelativeSource AncestorType={x:Type controls:PaletteStrip}}}"
                        Background="{Binding Converter={StaticResource RgbToBrush}}"
                        ToolTip="Click to remove this color"
                        Command="{Binding DataContext.RemovePaletteColorCommand,
                                  RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}"/>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
