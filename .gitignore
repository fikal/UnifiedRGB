bin/
obj/
*.user
.vs/

# native build artifacts (built locally via native/chroma-shim/build.bat)
native/chroma-shim/*.dll
native/chroma-shim/*.obj
native/chroma-shim/*.lib
native/chroma-shim/*.exp
native/chroma-shim/exports-*.txt

# maintainer-only publishing tool (not part of the public tree)
src/UnifiedRgb.Publisher/

# x86 shim resource compile output (build.bat regenerates it)
native/chroma-shim/version32.res

# x64 shim resource compile output (build.bat regenerates it on every run,
# and now does so in CI too - a tracked copy makes release.ps1 refuse to
# release on a "dirty tree" for a file nobody edited)
native/chroma-shim/version.res
